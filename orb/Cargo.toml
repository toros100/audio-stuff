[package]
name = "orb"
version = "0.1.0"
edition = "2024"


[dependencies]
crossbeam-utils = "0.8"
thiserror = "2"

[dev-dependencies]
rand = "0.10"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)'] }


[target.'cfg(loom)'.dependencies]
loom = "0.7"
