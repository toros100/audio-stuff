# wait-free spsc reordering queue

i made this to reorder opus audio packets

the implementation is sufficiently generic to reorder other things as well, but some choices were made specifically for that usage

(orb stands for opus reordering buffer 🔮)
